"""Persistence: SQLAlchemy mappings, engine management, and repositories.

This package is an adapter. It depends on the domain and implements the ports
the domain declares; the dependency never runs the other way.
"""
